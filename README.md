# E2E-tests

#### Tech stack

* Selenium WebDriver
* Maven
* TestNG

#### Preconditions:

* Need to install maven

#### Commands:

* For running tests use command:
  `mvn clean test`
* For generation allure report use command:
  `mvn allure:report`

**CI:** `gitlabCI`



